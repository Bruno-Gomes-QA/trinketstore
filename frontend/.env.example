# Run `openssl rand -base64 32` to generate a secret value in local dev.
NUXT_SESSION_PASSWORD=REPLACE_WITH_SECURE_RANDOM_VALUE

# Backend URL used by the server (internal)
NUXT_BACKEND_URL=http://localhost:8080/api
  
# Public backend URL exposed to the client
NUXT_PUBLIC_BACKEND_URL=http://localhost:8080/api
